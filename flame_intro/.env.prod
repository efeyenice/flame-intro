# Game Container
API_URL=http://backend:8000  # Or your production backend URL, e.g., https://your-domain.com/api

# Backend Container
DATABASE_URL=postgres://user:pass@db:5432/brickbreaker  # Full PostgreSQL connection string
PORT=8000  # Backend server port
CORS_ORIGIN=*  # Allowed origins (e.g., your frontend domain or '*' for development)

# Database Container
POSTGRES_DB=brickbreaker  # Database name
POSTGRES_USER=user  # Database username
POSTGRES_PASSWORD=pass  # Database password (use a strong, secure value in production)